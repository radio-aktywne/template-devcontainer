{
  "name": "devcontainer",
  "scripts": {
    "update": "ncu --peer --upgrade",
    "postupdate": "npm install --no-audit --no-fund",
    "dev": "devcontainer"
  },
  "devDependencies": {
    "@devcontainers/cli": "^0.52.0",
    "npm-check-updates": "^16.14.0"
  }
}
